#include <stdio.h>

int main() {
    // Iterate from 0 to 2 (3 times)
    for (int i = 0; i < 3; i++) {
        // Print "meow" on each iteration
        printf("meow\n");
    }

    // Return 0 to indicate successful completion
    return 0;
}
